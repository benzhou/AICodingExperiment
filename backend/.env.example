// filepath: /Users/ben.zhou/workspace/AICodingExperiment/backend/.env.example
DATABASE_URL=postgres://${DB_USER}:${DB_PASSWORD}@${DB_HOST:-localhost}:${DB_PORT:-5432}/${DB_NAME:-myapp}?sslmode=disable
FRONTEND_URL=http://localhost:3000
JWT_SECRET=development-secret-key-replace-in-production
JWT_EXPIRY_MINUTES=60
